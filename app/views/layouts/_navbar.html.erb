<nav class="navbar navbar-expand-lg navbar-light bg-light p-3 bg-dark px-5">
  <a class="navbar-brand text-white">Blood Glucose Trakcer</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <% if user_signed_in? %> 
    <ul class="navbar-nav mr-auto w-100">
      <li class="nav-item mt-1 ms-3">
        <%= link_to "Glucose Reading", glucose_readings_path, class: "nav-link text-white" %>
      </li>
      <li class="nav-item mt-1 ms-3">
        <%= link_to "Reports", reports_path, class: "nav-link text-white" %>
      </li>
    <div class="ms-auto">
      <%= button_to "Sign out", destroy_user_session_path, method: :delete  ,class:"btn btn-danger"%> 
    </div> 
      <% else %>
      <div class="ms-auto">
       <%= button_to "Sign in", new_user_session_path, class: "btn btn-primary" %> 
      </div>
      <% end %>
    </ul>
  </div>
</nav>